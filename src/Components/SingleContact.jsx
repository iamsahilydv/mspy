import { Box, Text } from "@chakra-ui/react";
import React from "react";

const SingleContact = (name) => {
  return (
    <Box>
      <Text>{name}</Text>
    </Box>
  );
};

export default SingleContact;
